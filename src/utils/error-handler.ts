// ✅ KEEP & MODIFY - Remove web-specific error handling
export const handleError = (error: unknown, fallback: string) => {
  // Remove web-specific error tracking
  // Keep basic error handling logic
}
